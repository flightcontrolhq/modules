- id: source_repo
  label: Git repository
  required: true
  show_when:
    build_type:
      - nixpacks
      - dockerfile
      - railpack
  type: gitrepo
- default: .
  description: Repository-relative source and build root.
  id: source_base_path
  label: Base path
  show_when:
    build_type:
      - nixpacks
      - dockerfile
      - railpack
  type: string

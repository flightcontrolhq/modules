apiVersion: v2
name: beacon-credential
description: The Kubernetes Secret holding Ravion Beacon's WorkOS Connect M2M credential, installed by the compute/eks/addons stack.
type: application
version: 0.1.0
